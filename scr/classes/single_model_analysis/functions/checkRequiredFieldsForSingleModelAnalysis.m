function [fieldsOK] = checkRequiredFieldsForSingleModelAnalysis(proj, modelList, analyses)
% Inside a model field, at least a model, but depends on what's necessary
% for the asked analyses

% FBA : model, parameters (including obj function, default/personalized table)
% FVA : model, parameters (idem + threshold)
% Sampling : model, parameters, path for saving 
% Single Gene Deletion : model, parameters (gene names or all)
% Double Gene deletion : model, paremeters (gene list 1 and 2)

end

